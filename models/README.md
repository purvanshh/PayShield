# Model Artifacts

Trained model artifacts and evidence outputs. The **return-risk** rows are the
evaluated submission surface; the GNN rows are archived **fraud-extension**
artifacts (see the README's Platform Extensions note).

## Contents

| Path | Description | Generated By |
|------|-------------|--------------|
| `gnn_benchmark_results.json` | GNN benchmark metrics (PR-AUC, AUC-ROC, latency, baseline) — archived fraud-extension artifact | the GNN benchmark pipeline (since removed) |
| `gnn_benchmark_results_original.json` | Archived v1.0.0 baseline results (for delta/improvement reports) | archived from git history |
| `gnn_benchmark_delta.md` | v1.0.0 → v1.1.0 comparison report — archived fraud-extension artifact | archived GNN delta tooling |
| `retrain_decision.json` | Last improvement-gate outcome (candidate vs production) | archived GNN gate tooling |
| `payshield_gnn_v1_card.md` | Full GNN v1.1.0 model card — archived fraud-extension artifact | curated card |
| `fairness_audit.py` | SPD/EOD on synthetic demographic slices | `python models/fairness_audit.py` |
| `production/calibrator_v1.pkl` | Fitted isotonic calibrator (ECE 0.010) | `engine/ensemble.py:ConfidenceCalibrator.fit()` |
| `production/payshield_gnn_v1.pt` | GNN v1.1.0 checkpoint (served by `ml/inference.py`) | archived GNN benchmark pipeline |
| `registry/` | Versioned model artifacts — v1.1.0 (GNN), v1.0.0 (statistical filter), v0.1.0 (GNN legacy) | archived GNN registry tooling |
| `return_risk_xgb_best.json` | **Tuned XGBoost return-risk model** (Stage 1: PR-AUC 0.8089 on raw features with a non-circular hidden-confounder DGP) — primary engine loaded by `return_risk/scorer.py` | `scripts/tune_xgb.py` |
| `return_risk_xgb_v1.json` | XGBoost return-risk model with default hyperparameters (PR-AUC 0.7991, fallback if `best` is absent) | `scripts/train_xgb_return_risk.py` |
| `xgb_evaluation.json` | Phase 1 comparison — XGBoost vs hand-weighted vs naive baselines (PR-AUC/ROC-AUC/precision/recall/F1/confusion matrix) | `scripts/train_xgb_return_risk.py` |
| `ablation_study.json` | Leave-one-feature-out ablation (PR-AUC drop per feature, baseline 0.8087) | `scripts/ablation_study.py` |
| `xgb_tuning_results.json` | Legacy 144-combo grid-search results (superseded by `tune_results_{scenario}.json`) | `scripts/tune_xgb.py` |
| `return_risk_results_{basic,enriched,premium}.json` | Per-scenario measured metrics + operating curve (default XGBoost) | `scripts/train_xgb_return_risk.py --scenario {s}` |
| `tune_results_{basic,enriched,premium}.json` | Per-scenario HalvingGridSearchCV results + best params + test metrics | `scripts/tune_xgb.py --scenario {s}` |
| `cost_model_results.json` | Cost model with legacy `scenarios` + `maturity_scenarios` (3-stage × 2-vertical table) | `docs/cost_model/calculator.py --all-maturity` |

## Fallback Behavior

When no production model is available:

- **L1 (Statistical Filter)**: Runs rule-based velocity/geo/Benford checks (no model required)
- **L2 (GNN)**: Returns `MODEL_UNAVAILABLE` — ensemble falls back to L1-only fusion. No `type("L2",...)()` stub — real inference since Phase 3.
- **Ensemble**: Weighted fusion adapts to available layers; isotonic calibrator loaded if `calibrator_v1.pkl` exists
