apiVersion: 1

providers:
  - name: PayShield
    orgId: 1
    folder: PayShield
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
