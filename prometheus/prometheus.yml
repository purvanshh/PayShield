global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: payshield-api
    metrics_path: /metrics
    static_configs:
      - targets: ["localhost:8000"]
        labels:
          service: payshield-api
