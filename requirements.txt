# Fully pinned to the canonical Python 3.11 stack (the Docker base and the
# documented verify interpreter). Every version below is what
# .venv-verify resolves to — a fresh clone installs byte-identical deps, so
# the --full-verify numbers and the live stack reproduce exactly.
#
# The ML five (numpy/pandas/scipy/scikit-learn/xgboost) are the reproducibility
# contract for the AUC/ablation gates — keep them in lockstep with
# models/return_risk_results_*.json, models/ablation_study.json and
# docs/_number_manifest.json.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
redis==8.1.0
torch==2.12.0
torch-geometric==2.8.0.post1
networkx==3.6.1
numpy==2.2.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.9.0
xgboost==3.2.0
prometheus-client==0.26.0
structlog==26.1.0
jinja2==3.1.6
httpx==0.28.1
pyyaml==6.0.3
ollama==0.6.2
shap==0.51.0
celery==5.6.3
faker==40.37.0
python-dateutil==2.9.0.post0
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
locust==2.46.4
requests==2.34.2
aiohttp==3.14.3
sqlalchemy==2.0.52
asyncpg==0.31.0
neo4j==6.3.0
sse-starlette==3.4.8
PyJWT==2.13.0
