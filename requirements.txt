fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.0.0
redis>=5.0.0
torch>=2.2.0
torch-geometric>=2.5.0
networkx>=3.2.0
# Pinned exactly — the `--full-verify` suite's AUC/ablation checks are only
# reproducible on this canonical stack. These versions are the newest with
# binary wheels on BOTH macOS and Linux for Python 3.11 (the Docker base and
# the documented verify interpreter). Floating pins let a fresh clone resolve
# newer libs and drift below the tight gates (e.g. premium PR-AUC 0.9497 vs the
# >=0.94 gate is a 0.0097 margin). Keep these in lockstep with the values in
# models/return_risk_results_*.json, models/ablation_study.json and
# docs/_number_manifest.json. Verified stack: Python 3.11.
numpy==2.2.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.9.0
xgboost==3.2.0
prometheus-client>=0.19.0
structlog>=24.1.0
jinja2>=3.1.0
httpx>=0.27.0
pyyaml>=6.0.0
ollama>=0.1.0
shap>=0.44.0
celery>=5.4.0
faker>=22.0.0
python-dateutil>=2.9.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
locust>=2.24.0
requests>=2.32.0
aiohttp>=3.10.6
sqlalchemy>=2.0.0
asyncpg>=0.29.0
neo4j>=5.19.0
sse-starlette>=2.0.0
PyJWT>=2.10.1
